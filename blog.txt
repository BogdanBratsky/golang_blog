// POST '/register' - регистрация пользователя

// POST '/login' - авторизация пользователя

// GET '/users' - получение всех пользователей

// GET '/posts' - получение всех постов всех пользователей

// GET '/users/<user_id>' - получение конкретного пользователя

// DELETE '/users/<user_id>' - удаление конкретного пользователя

// GET '/users/<user_id>/posts' - получение всех постов пользователя

// GET '/users/<user_id>/<post_id>' - получение конкретного поста пользователя

// ! POST '/users/<user_id>/posts' - создаёт пост

// ! DELETE '/users/<user_id>/<post_id>' - удаление поста

// ! PATCH '/users/<user_id>/<post_id>' - редактирование поста

// GET '/categories' - получение всех категорий

// GET '/categories/<category_id>' - получение конкретной категории

// GET '/tags' - получение всех тегов

// GET '/tags/tag_id' - получение конкретного тега